<form method="post" action="{{ path('projet_delete', {'id': projet.id}) }}" onsubmit="return confirm('Voulez-vous supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ projet.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
