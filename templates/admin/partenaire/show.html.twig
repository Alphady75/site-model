{% extends 'base.html.twig' %}

{% block title %}Partenaire
{% endblock %}

{% block body %}
	<h1>Partenaire</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>#ID</th>
				<td>{{ partenaire.id }}</td>
			</tr>
			<tr>
				<th>Name</th>
				<td>{{ partenaire.name }}</td>
			</tr>
			<tr>
				<th>Logo</th>
				<td>{{ partenaire.logo }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('partenaires_index') }}">back to list</a>

	<a href="{{ path('partenaires_edit', {'id': partenaire.id}) }}">edit</a>

	{{ include('admin/partenaire/_delete_form.html.twig') }}
{% endblock %}
