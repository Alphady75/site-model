<form method="post" class="d-inline" action="{{ path('membre_delete', {'id': membre.id}) }}" onsubmit="return confirm('Voulez-vous supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ membre.id) }}">
    <button class="btn btn-danger"><i class="fa fa-trash-alt"></i> Supprimer</button>
</form>
