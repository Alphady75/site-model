{% extends 'admin.html.twig' %}

{% block title %}Service{% endblock %}

{% block body %}
    <h1>Service</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ service.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ service.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ service.description }}</td>
            </tr>
            <tr>
                <th>Created</th>
                <td>{{ service.created ? service.created|ago : '' }}</td>
            </tr>
            <tr>
                <th>Updated</th>
                <td>{{ service.updated ? service.updated|ago : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('service_index') }}">back to list</a>

    <a href="{{ path('service_edit', {'id': service.id}) }}" class="btn btn-light"><i class="fa fa-edit"></i> Mettre à jour</a>

    {{ include('admin/service/_delete_form.html.twig') }}
{% endblock %}
