{% extends 'admin.html.twig' %}

{% block title %}Mise à jour le service #{{ service }}{% endblock %}

{% block body %}

<div class="row">
    <div class="col-sm-12 mx-auto p-4">
        <h1>Mise à jour le service #{{ service }}</h1>
        {{ include('admin/service/_form.html.twig', {'button_label': 'Enregistrer les modifications'}) }}
    </div>
</div>

{% endblock %}
