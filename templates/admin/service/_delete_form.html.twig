{% if is_granted('ROLE_ADMIN') %}
	<form method="post" class="d-inline" action="{{ path('service_delete', {'id': service.id}) }}" onsubmit="return confirm('Voulez-vous supprimer cet élément?');">
		<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ service.id) }}">
		<button class="btn btn-danger">
			<i class="fa fa-trash"></i>
		</button>
	</form>
{% endif %}
