<form method="post" action="{{ path('service_delete', {'id': service.id}) }}" onsubmit="return confirm('Voulez-vous supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ service.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
