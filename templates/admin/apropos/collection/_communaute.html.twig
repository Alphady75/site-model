<div class="mb-3 border radius p-3">
	<div class="row">
		<div class="col-sm-6">{{ form_row(form.name) }}</div>
		<div class="col-sm-6">{{ form_row(form.icon) }}</div>
		<div class="col-sm-12">{{ form_row(form.lien) }}</div>
	</div>
	<button type="button" class="btn-remove btn btn-outline-danger btn-sm">
		<i class="fa fa-trash"></i>
	</button>
</div>
