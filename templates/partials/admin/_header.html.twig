<div class="header border-bottom">
  <div class="header-left">
    <div class="menu-icon dw dw-menu"></div>
  </div>
  <div class="header-right">
    <div class="user-info-dropdown">
      <div class="dropdown">
        <a class="dropdown-toggle" href="#" role="button" data-toggle="dropdown">
          <span class="user-icon">
            <img src="{% if apropo().logo %}{{ vich_uploader_asset(apropo(), 'logoFile') }}{% else %}{{ asset('img/document.svg') }}{% endif %}" alt="">
          </span>
          <span class="user-name">{{ app.user }}</span>
        </a>
        <div class="dropdown-menu dropdown-menu-right dropdown-menu-icon-list">
          <a class="dropdown-item" href="{{path('user_edit', {id: app.user.id})}}"><i class="dw dw-user1"></i> Profile</a>
          <a class="dropdown-item" href="{{ path('app_logout') }}"><i class="dw dw-logout"></i> Déconnexion</a>
        </div>
      </div>
    </div>
  </div>
</div>