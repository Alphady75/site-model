<div class="container-xxl py-5">
	<div class="container">
		<div class="row align-items-end mb-5">
			<div class="col-lg-6 wow fadeInUp" data-wow-delay="0.1s">
				<div class="border-start border-5 border-primary ps-4">
					<h6 class="text-body text-uppercase mb-2">Notre équipe</h6>
					<h1 class="display-6 mb-0">Notre travailleur expert</h1>
				</div>
			</div>
		</div>
		<div class="row g-4">
			{% for membre in membres() %}
			<div class="col-lg-4 col-md-6 mx-auto wow fadeInUp" data-wow-delay="0.1s">
				<div class="team-item position-relative">
					<img class="img-fluid" src="{% if membre.image %}{{ vich_uploader_asset(membre, 'imageFile' ) | imagine_filter('large') }}{% else %}{{ asset('img/img-def-min.png') | imagine_filter('large') }}{% endif %}" alt="{{ membre }}"/>
					<div class="team-text bg-white p-4">
						<h5>{{ membre }}</h5>
						<span>{{ membre.fonction }}</span>
					</div>
				</div>
			</div>				
			{% endfor %}
		</div>
	</div>
</div>
