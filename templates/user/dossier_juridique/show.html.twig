{% extends 'base.html.twig' %}

{% block title %}DossierJuridique
{% endblock %}

{% block body %}
	<h1>DossierJuridique</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ dossier_juridique.id }}</td>
			</tr>
			<tr>
				<th>Name</th>
				<td>{{ dossier_juridique.name }}</td>
			</tr>
			<tr>
				<th>Etat</th>
				<td>{{ dossier_juridique.etat }}</td>
			</tr>
			<tr>
				<th>Created</th>
				<td>{{ dossier_juridique.created ? dossier_juridique.created|date('Y-m-d H:i:s') : '' }}</td>
			</tr>
			<tr>
				<th>Updated</th>
				<td>{{ dossier_juridique.updated ? dossier_juridique.updated|date('Y-m-d H:i:s') : '' }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('dossier_juridique_index') }}">back to list</a>

	<a href="{{ path('dossier_juridique_edit', {'id': dossier_juridique.id}) }}">edit</a>

	{{ include('user/dossier_juridique/_delete_form.html.twig') }}
{% endblock %}
