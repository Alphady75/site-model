<header
	class="pb-3">
	<!-- Header Top -->
	<nav class="navbar navbar-expand-lg navbar-dark bg-primary border-bottom p-sm-2">
		<div class="container">
			<a class="navbar-brand align-items-center d-flex p-0 m-0" data-turbo="false" href="{{path('accueil')}}">
				<img src="{{asset('img/logo.png')}}" width="50" alt="">
				<strong class="text-uppercase font-weight-bold text-light">{{appName}}</strong>
			</a>
			<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
				<span class="navbar-toggler-icon"></span>
			</button>
			<div class="collapse navbar-collapse" id="navbarText">
				<ul class="navbar-nav ml-auto">
					<li class="nav-item">
						<a class="nav-link" href="#">{{demande.demarche}}
							<strong class="font-weight-bold text-light">{{demande.statut ? ' : ' ~ demande.statut : ''}}</strong>
						</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="#">REF
							<strong class="font-weight-bold text-light">{{demande.id}}</strong>
						</a>
					</li>
				</ul>
				<div class="navbar-text">
					<a href="tel:{{apropo().telephone}}" class="btn btn-light text-dark">
						<i class="fa fa-phone"></i>
						{{apropo().telephone}}
						<span class="text-right" style="display: inline-block;width:20px">
							<img src="{{asset('img/flag.jpg')}}" width="20" alt="">
						</span>
					</a>
					{% if app.user %}
						<a href="{{path('user_demande_index')}}" class="btn btn-light text-dark">
							<i class="fa fa-user-circle"></i>
							Espace
							{{app.user.compte}}
						</a>
					{% endif %}
				</div>
			</div>
		</div>
	</nav>
	<!-- Header Top -->
	{% include "demande/composants/_steps.html.twig" %}
</header>
<!--End Main Header -->
